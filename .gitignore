# The `rutba` meta-repo owns only the estate root. The three tiers are their
# own git repositories — see REPOS.md for the full repo ⇄ directory map.
/consumer/
/workers/
/management/
/native-apps/

node_modules/
*.log

.env
.env.*
!.env.example
!.env.*.example

# Agent scratch and per-machine settings.
.ai/
.vs/

# Working notes and deployment write-ups kept on this machine only. Operational
# detail about the live estate — hostnames, ports, patch lists — belongs on the
# box it describes (/opt/rutba/DEPLOYMENT-README.md), not in a public repo.
.local/
