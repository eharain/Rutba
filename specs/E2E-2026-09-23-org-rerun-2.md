# E2E-ORG re-run 2 — from the team organisation to the colleague (2026-09-23)

Thread E2E-ORG, third run, of the two-instance end-to-end programme
([e2e-two-instances.md](e2e-two-instances.md)). It follows the first run
([E2E-2026-09-23-org.md](E2E-2026-09-23-org.md)), the second
([E2E-2026-09-23-org-rerun.md](E2E-2026-09-23-org-rerun.md), which stopped at
the conversion of organisation 140), the setup record
([E2E-2026-09-23-setup.md](E2E-2026-09-23-setup.md)) and the review
([REVIEW-2026-09-23-e2e.md](REVIEW-2026-09-23-e2e.md)). It tests; it changes no
code, no environment file and no service, and it wrote no row in the management
database. Every step is PASS, FAIL or BLOCKED with the evidence beside it.

## Estate tips at start

Read at 11:07 UTC.

| Repo | Branch | Tip |
|---|---|---|
| records (`D:/Rutba2.0`) | dev | `ed05af705cd974a3588183eccf6c6b9a300f9b2e` — E2E-ORG re-run: steps 4 to 7 blocked on the conversion |
| consumer | dev | `0aaf37ff5bf5cfb5d53dbad10c158280e22a0bc7` — fleet: fence performance_schema with the five privileges it accepts |
| management | dev | `7fabfcc204816600a13fa661ccaa63693267a22a` — merge ws/c: the caller on every minted credential |
| workers | dev | `f0f5e8d6933fc15d4d41eb876a7445b357d16529` — provisioning: a database the core cannot open is not an instance |

All four working trees were clean. Estate (`http://localhost:4999/status.json`
at 11:08 UTC): profile `erp`, `6 running · 46 asleep`; `erp-auth:4003`,
`erp-core:4020`, `erp-sign:4029`, `gateway:4100`, `auth:4101` and
`management-strapi:4116` ready.

Times are UTC unless a log line carries the estate's own clock (UTC+5, the
`16:xx` stamps). The thread started at 16:07 local, so its marker is `1607`
and its new addresses are `e2e-org3-1607-<role>@rutba.test`.

## Environment

Nothing in any repository, no environment file and no service was changed by
this thread, and it wrote nothing in management Strapi's database (every read
there went through a helper that refuses anything but a `select`, inside a
read-only transaction). `consumer/.data/tenants.dev.json` was read and never
written by this thread; the tenants door wrote it (step 2). Scripts ran from
this session's scratchpad, folder `e2e-org-rerun-2/` (transcripts under
`out/`); every secret was read from the estate's files inside the process that
used it and none is reproduced here or in a transcript.

- **Management Strapi** was restarted by the lead as this thread began (its
  boot finished at 16:07:04 local). Its `.env` reads `MAIL_TRANSPORT=log`, and
  its boot log shows `[bus] consuming {"group":"provisioning.subscriptions","patterns":["subscription.*"]}`
  and `[control-plane] hosting 3 reaction(s) and 9 schedule(s)`.
- **The worker's environment**: `D:/Rutba2.0/.env.local` carries fifteen
  `PROVISIONING_WORKER__*` lines (lines 107 to 121). The non-secret values:
  `STRAPI_URL=http://127.0.0.1:4116`, `CORE_URL=http://127.0.0.1:4020`,
  `CORE_AUDIENCE=http://localhost:4003`, `RUTBA_CONSUMER_ROOT` the consumer
  checkout, `PROVISIONING_DEFAULT_CELL=dev`, `TEMPLATE_SOURCE_DB=pos_db`,
  `PROVISIONING_TEMPLATE_DEFAULT=tpl_sign`,
  `PROVISIONING_LAUNCHER_URL=http://localhost:4003/?db={db}`,
  `PROVISIONING_REALM_ISSUER=http://localhost:4020`, `_JWKS_URI=http://localhost:4020/.well-known/jwks.json`,
  `_AUTHORIZE=http://localhost:4003`, `PROVISIONING_LEASE_MS=900000`. The
  token and the two cell credentials were passed and never printed.
- **Row 47** reads `status deleted`; row 106 (`individual_dev`) `active` (read
  11:10 UTC).

How the new tenant was addressed: **only by a token.** Its directory entry has
`"domains": []` (step 2), so no `X-Rutba-Domain`, with or without the edge key,
names it; every call below carries a token whose `db` claim is
`sign_e2eorg0145owner12d3`, minted by the core's
`POST /api/auth/local/any`, by the reset door, or by the bridge, plus the
api-pro claim headers (`X-Rutba-App`, `X-Rutba-App-Role`) the apps send.

## Step 1 — the conversion (the owner's), and the gate

**Done by the owner; the gate held and then opened.** This thread did not write
the row. It polled `select id, kind, name, updated_at from organizations where id = 140`
every two minutes (`out/gate-poll.txt`):

```
11:08:15Z  {"id":140,"kind":"personal","name":"E2E Org Owner","updated_at":"2026-09-23T10:57:10.923Z"}
11:10:38Z  {"id":140,"kind":"personal", …}
11:12:38Z  {"id":140,"kind":"team","name":"E2E Org2 1543 Ltd","updated_at":"2026-09-23T11:12:08.459Z"}   GATE OPEN
```

The conversion stays manual: nothing in the estate turns a personal
organisation into a team one (the previous record's defect 1 stands).

## Step 2 — buy `sign.subscription`, run the worker once — PASS

### Before (11:12:52 to 11:14:04 UTC, `out/s2-before.txt`, `out/s2-directory-before.txt`)

- organisation 140: `org_c2791c709b12b1fb`, slug `e2e-org-0145-owner-12d3`,
  `kind team`, `status active`; one membership (148, up_users 178, active); no
  subscription; no verified domain (`domains_organization_lnk` empty).
- catalogue: product `sign` is `shared-tenant`; plan `sign.subscription` is
  active.
- the queue: no job `queued`, `running` or `rolling_back` anywhere.
- the directory file: two tenants (`pos_db`, `individual_dev`), sha256
  `95f2df52…c54a`, last written 01:28:04 local by E2E-SETUP.
- the cell (MySQL 127.0.0.1:3306): `individual_dev`, `pos_db`, `tpl_sign`, no
  `sign_%` database.

### The purchase (`out/s2-buy.txt`, runner `s2-buy.mjs`)

The hub has no checkout for `sign.subscription` (previous record, observation
1) and the portal console is not in this profile, so the purchase went through
the Strapi door the walkthrough uses
(`management/api/legacy/strapi/scripts/provisioning-walkthrough.js` lines 367
to 384), with the owner's own session:

```
POST http://localhost:4101/login  {email, password}                 303 -> http://localhost:4118/auth/signin?next=%2F&org=e2e-org-0145-owner-12d3, cookie rutba_sid
POST http://localhost:4101/v1/auth/token {org_id: org_c2791c709b12b1fb, app: portal, azp: portal-console}
     200  org {"id":"org_c2791c709b12b1fb","slug":"e2e-org-0145-owner-12d3","plan":"starter","name":"E2E Org2 1543 Ltd"} roles ["owner"]
          claims {"iss":"http://localhost:4101","aud":"api.rutba.io","azp":"portal-console","sub":"usr_144c1d5021c8531f"}
POST http://127.0.0.1:4116/api/portal/subscriptions {"plan_code":"sign.subscription","quantity":1}
     Authorization: Bearer <GATE_TOKEN_PORTAL_CONSOLE>   x-rutba-user-token: <the portal token above>
     201 at 11:13:23.996Z  subscription id 94, sub_a37c9d7dcfe44457b362, active, monthly, USD, amount 2900 minor,
         period 2026-09-23T11:13:23.515Z to 2026-10-23T11:13:23.515Z, organisation 140 kind team
```

Strapi's log (`out/strapi-log-s2-buy.txt`):

```
[16:13:23.995] http: POST /api/portal/subscriptions (660 ms) 201
[16:13:25.001] info: [control-plane] reaction provisioning.subscription-changed ran for subscription.created evt_659PNFHdxDBoIrW6 {"queued":"job_df84224e76bff58bda","staff":false,"cell":"k8s-dev-1"}
[16:13:25.045] info: [licence] sign for org_c2791c709b12b1fb: lic_4f1d79bd0cd04d0684e119e4 (subscription.created)
```

- **licence** 127, `lic_4f1d79bd0cd04d0684e119e4`, plan `sign.subscription`,
  `active`, issued 11:13:24.879Z, expires 2026-10-23T11:13:23.515Z, grace 7
  days, `entitlements {"quotas":{},"modules":[]}` (`out/s2-licence.txt`).
- **provision job** 162, `job_df84224e76bff58bda`, `action provision`,
  `status queued`, `step null`, `attempts 0`, `run_after null`, input
  `{"cell":"k8s-dev-1","orgId":"org_c2791c709b12b1fb","productKey":"sign","environment":"live","subscriptionId":"sub_a37c9d7dcfe44457b362"}`,
  created 11:13:24.951Z (`out/s2-job-queued.txt`). At 11:14:29Z it was the
  only open job on the estate, so it was the job the one-shot run would claim
  (`out/s2-queue-before-worker.txt`).
- outbox: 216 `invoice.created`, 217 `subscription.created`.

### The worker, once (`out/s2-worker.log`, runner `s2-worker.mjs`)

`node src/index.js --once` in `workers/provisioning`, its environment the
fifteen lines with the prefix stripped plus `WORKER_NAME=e2e-org3-1607-worker`
(a run parameter). It claimed job 162 and finished `succeeded`, exit 0, in 191
seconds:

| Step | Done at (UTC) | Took | What it recorded |
|---|---|---|---|
| claim | 11:14:36.39 | | `POST /api/worker/provisioning/claim` 200 |
| `naming` | 11:14:38.81 | 2.6 s | `db sign_e2eorg0145owner12d3`, cell `k8s-dev-1`, slug `e2e-org-0145-owner-12d3`, `fresh true` |
| `create_database` | 11:17:28.94 | 170 s | `created true`, `cloned true`, template `tpl_sign`, 1272 tables, 0 views, 14 tables seeded, engine mysql, host 127.0.0.1:3306 |
| `migrate` | 11:17:43.80 | 15 s | `[migrate] database: mysql2 127.0.0.1:3306/sign_e2eorg0145owner12d3 [ENVIRONMENT=development]` / `[migrate] nothing pending` |
| `register` | 11:17:44.13 | 0.3 s | `registered true`, `already false`; the credential minted through `POST /api/worker/provisioning/token` (200, 16:17:43.988) |
| `owner` | 11:17:46.48 | 2.4 s | `bootstrapped true`, `email e2e-org-0145-owner@rutba.test`, `already false` |
| `record` | 11:17:46.52 | | the instance below |
| `sign_key` | 11:17:46.72 | 0.2 s | `written true`, `keyId sik_a944958983fd703f8c` (Strapi: `[sign] issued Sign key sik_a944958983fd703f8c for sign_e2eorg0145owner12d3`) |
| succeed | 11:17:46.83 | | `POST …/succeed` 200; outbox 218 `instance.ready` at 11:17:46.801Z |

The job row afterwards: `status succeeded`, `step sign_key`, `attempts 0`,
`error null`, the whole `state.steps` as above, and no credential in it
(`out/s2-after-rows.txt`).

The consumer core's log for the same minute (`out/s2-core-log.txt`; instance
mail is in log mode):

```
16:17:44.053   53ms 201 POST http://127.0.0.1:4020/api/tenants
[email] (log mode) to=e2e-org-0145-owner@rutba.test subject="Your Rutba space is ready"
16:17:46.429 2298ms 201 POST http://127.0.0.1:4020/api/tenants/sign_e2eorg0145owner12d3/owner
16:17:46.696   29ms 200 PUT  http://127.0.0.1:4020/api/tenants/sign_e2eorg0145owner12d3/platform-keys
```

### What it made

**The directory** (`out/s2-directory-after.txt`, read 11:18:25Z): the file was
rewritten by the door at 16:17:44.022 local, sha256 `01fc8a6c…3a48`, the
`$comment` head and both earlier entries unchanged, and a third entry:

```json
{ "db": "sign_e2eorg0145owner12d3", "name": "E2E Org2 1543 Ltd", "mode": "organisation",
  "orgId": "org_c2791c709b12b1fb", "domains": [] }
```

with a storage folder `consumer/.data/tenants/sign_e2eorg0145owner12d3/`. The
running core serves it without a restart: `GET http://localhost:4020/health`
answers `ok` with `sign_e2eorg0145owner12d3: database ok, schema ok` beside the
other two. The entry names **no domain**, because the organisation has none;
see defect 2.

**The instance record**, `tenant_instances` id **107**, document
`zdg5mmxff0t3adg44ptjatww`, product `sign`, organisation 140:

```
tenant_ref sign_e2eorg0145owner12d3 · tier shared · status active · environment live · label null
url      http://localhost:4003/?db=sign_e2eorg0145owner12d3
auth     {"api":"http://127.0.0.1:4020","mode":"organisation","issuer":"http://localhost:4020","handoff":true,
          "jwks_uri":"http://localhost:4020/.well-known/jwks.json","authorize":"http://localhost:4003"}
database {"cell":"k8s-dev-1","host":"127.0.0.1","port":3306,"engine":"mysql","database":"sign_e2eorg0145owner12d3"}
compute  {"core":"http://127.0.0.1:4020","endpoint":"http://localhost:4003/?db=sign_e2eorg0145owner12d3"}
created  2026-09-23T11:17:46.553Z
```

and `sign_instance_keys` id 19, `sik_a944958983fd703f8c`, `active`, linked to
it.

**The owner bootstrap**, in the new database (`out/s2-tenant-rows.txt`, read
11:18:56Z):

```
up_users 1  e2e-org-0145-owner@rutba.test  document maamholggwxgp8o0o2wf2t7a  confirmed 1  blocked 0
            rutba_sub usr_144c1d5021c8531f  reset_password_token present  users-permissions role rutba_app_user
app roles   154 (every role in the catalogue: *_admin, *_manager, *_staff for each app, plus
            storefront_public, storefront_user, shopfloor_device, timeclock_device, ess_employee, …)
store       platform.sign_key  {value_enc: "v1:…" sealed, issued_by null, updated_at 11:17:46.672Z}
counts      users 1 · app roles 154 · migrations 110
users-permissions advanced
            {"allow_register":true,"email_confirmation":true,"email_reset_password":null,
             "email_confirmation_redirection":"http://localhost:4000/login?confirmed=1","default_role":"authenticated"}
```

The owner holding all 154 roles is the setup module's bootstrap as written
(`provisionOwner` reports `rolesTotal`; `consumer/console/api/tenants/domain/people.js`
lines 157 to 199). The open register door, the storefront confirmation
redirect and the unset reset page are the template's, carried into the first
provisioned organisation: **expected, review finding 9**, now live on a bought
instance.

## Step 3 — the hub, the new workspace tile, the bridge — PASS

`GET http://localhost:4101/hub` with the owner's session (`out/s3-hub.txt`, 11:19:55Z):

```
GET /hub -> 200     marker: localhost · AUTH-HUB · 0.1.0 · e098f06c
headings: "Hello, E2E" · "E2E Org2 1543 Ltd / Owner" · "1 What you have" · "2 Add a service"
tile: Rutba Sign · Live · Sign workspace  -> /hub/open/zdg5mmxff0t3adg44ptjatww?t=<signature>
```

One tile, for instance 107. The Individuals tile the personal organisation
showed is gone now the organisation is a team. "Account and billing" and most
products' plans still link to the portal console on 4118 (not running).

The tile's hop (`GET /hub/open/zdg5mmxff0t3adg44ptjatww?t=…`) answered `303` to

```
http://localhost:4003/authorize?redirect_uri=http://localhost:4003/auth/callback&state=<…>
    &login_hint=e2e-org-0145-owner@rutba.test&tenant=sign_e2eorg0145owner12d3&code=<…>
```

and auth logged `{"workspace":"zdg5mmxff0t3adg44ptjatww","bridged":true,"msg":"hub: opening a workspace"}`;
the core logged `200 POST http://127.0.0.1:4020/api/auth/handoff`. The first
code (11:19:57Z) was left unspent while this thread checked the browser tab;
a second was minted at 11:20:48Z and opened at once.

**In the browser** (`out/s3-launcher.txt`): a new tab of this thread's own
(`tab-3`), its 4003 `localStorage`, `sessionStorage` and cookies read empty
first, then navigated to the bridge link. The core logged
`16:20:56.520 200 POST http://localhost:4020/api/auth/handoff/redeem`, and at
11:21:11Z the tab was on `http://localhost:4003/?db=sign_e2eorg0145owner12d3`,
title "Rutba Suite", hydrated (`#__next` carries its `__reactContainer` key,
`router.isReady` true, `visibilityState` visible), `sessionStorage` holding
`jwt`, `refreshToken`, `user`, `rolesByApp` and six more:

```
Welcome back, E2E Org Owner
You have access to 39 apps. Pick one to get going.
SALES & CUSTOMERS  CRM · Helpdesk · Marketplace · Point of Sale · Web Orders · Affiliate Program
MARKETING & WEB    Campaigns · Content Management · Social Media · Studio
… MANUFACTURING, LOGISTICS & FLEET, FACILITIES & ASSETS, PEOPLE & PAYROLL, FINANCE & ACCOUNTING,
MAIL, CHAT & CALLS, DOCUMENTS & SIGN (Workspace, Sign, Assistant), ADMINISTRATION (Rutba Admin, Seeding)
```

**The bridge signed the owner in without a password**, before the owner had
ever set one. The session row in the new tenant (`out/s3-sessions.txt`, read
11:21:25Z):

```
strapi_sessions 3  users-permissions refresh active 11:20:56.482Z
  {"amr":["management-handoff"],"sub":"usr_144c1d5021c8531f","purpose":"open",
   "allowance":{"sub":"usr_144c1d5021c8531f","quotas":{},"source":"bridge","entitlements":["sign"]},
   "entitlements":["sign"],"management_sub":"usr_144c1d5021c8531f","deviceName":"Chrome on Windows"}
strapi_sessions 1  users-permissions handoff pending 11:19:57.005Z   (the unspent first code)
```

The session carries the licence's product, `entitlements ["sign"]`, and the
launcher still offers 39 apps (defect 1).

## Step 4 — the owner's set-password link, and the instance's own sign-in — PASS

The mail is `to=e2e-org-0145-owner@rutba.test subject="Your Rutba space is ready"`
and nothing else in the log (expected, the review's lower note on
`consumer/api/platform/src/email.js`). The link it carried is built by
`issueSetPasswordLink` (`consumer/console/api/tenants/domain/people.js` lines
94 to 116): `http://localhost:4003/login?code=<128 hex>&invited=1` — it names
no tenant. The token was read from the owner's row (never printed) and spent
at the door the login page posts to, with the page's own body
(`consumer/console/apps/auth/pages/login.js` lines 280 to 293), so the
tenant was not named either (`out/s4-set-password.txt`, 11:22:27Z):

```
POST /api/auth/reset-password/any {code, password, passwordConfirmation}    200  a session for user 1 (tokens not recorded)
up_users 1 afterwards: reset_password_token cleared, confirmed 1
POST /api/auth/local/any {identifier, password}           200 tenant {"db":"sign_e2eorg0145owner12d3","name":"E2E Org2 1543 Ltd","domain":null}
POST /api/auth/local/any {identifier, password, db}       200 the same; claims {"userId":"1","db":"sign_e2eorg0145owner12d3","type":"access"}
GET  /api/users/me                                        200 {"id":1,"email":"e2e-org-0145-owner@rutba.test","confirmed":true}
```

The `/any` door found the tenant by the code alone, so the link works on a
directory core although it names none. The instance's own sign-in was driven
at the door its form posts to (`/api/auth/local/any`), not typed into the
browser page: this thread does not type passwords into a browser.

## Steps 5 to 7

Recorded in the second half of this record.
